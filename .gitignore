# Session-scoped scratch: evidence artifacts, trace logs, task ledger.
# Written by ShipShape's own wrappers and hooks; never part of the source.
.shipshape/

# Test scratch
tmp/

# Internal docs — design notes, audits, plans, retros. Excluded here rather than
# only in the operator's global ignore file, so a fresh clone on any machine
# cannot stage them by accident.
docs/superpowers/

.DS_Store
